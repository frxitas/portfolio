import React, { FC } from "react";

import * as Styles from "./Header.styles";

const Header: FC = () => {
	return (
		<Styles.Header>
			<Styles.HeaderContent>Header</Styles.HeaderContent>
		</Styles.Header>
	);
};

export default Header;
